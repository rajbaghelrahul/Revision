// const add = (a, b) => a + b;
// const subtract = (a, b) => a - b;
// const multiple = (a, b) => a * b;
// const divide = (a, b) => a / b;

// module.exports = { add, subtract, multiple, divide };

            
// OR


exports.add = (a, b) => a + b; // It is similar as above format but its like a anonymous funcion.
exports.subtract = (a, b) => a - b; // It is similar as above format but its like a anonymous funcion.
exports.multiple = (a, b) => a * b; // It is similar as above format but its like a anonymous funcion.
exports.divide = (a, b) => a / b; // It is similar as above format but its like a anonymous funcion.

// module.exports = { add, subtract, multiple, divide };




